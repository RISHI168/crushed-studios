// BullMQ job processor for generate-audio
import { Job } from 'bullmq'

export async function processGenerate-audio(job: Job) {
  // TODO: Implement generate-audio processor
  console.log(`Processing generate-audio with data:`, job.data)
}
