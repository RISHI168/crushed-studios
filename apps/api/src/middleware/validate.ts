// validate middleware
import { FastifyRequest, FastifyReply } from 'fastify'

export async function validateHook(request: FastifyRequest, reply: FastifyReply) {
  // TODO: Implement validate middleware logic
}
